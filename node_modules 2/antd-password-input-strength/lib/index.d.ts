import React from "react";
import { InputProps } from "antd/lib/input";
export declare class PasswordInput extends React.Component<PasswordInputProps> {
    static defaultProps: Partial<PasswordInputProps>;
    state: {
        level: number;
    };
    onChange: (e: React.ChangeEvent<HTMLInputElement>) => void;
    render(): JSX.Element;
}
export declare const PasswordStrengthIndicator: ({ level, settings }: PasswordStrengthIndicatorProps) => JSX.Element;
export interface PasswordInputProps {
    settings?: PasswordInputSettings;
    onChange?: (e: React.ChangeEvent<HTMLInputElement>) => void;
    inputProps?: InputProps;
}
export interface PasswordInputSettings {
    colorScheme: ColorScheme;
    height: number;
    alwaysVisible: boolean;
}
interface PasswordStrengthIndicatorProps {
    level: number;
    settings: PasswordInputSettings;
}
export interface ColorScheme {
    levels: string[];
    noLevel: string;
}
export {};
